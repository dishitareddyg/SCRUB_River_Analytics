"""Configuration package.

Exposes the application settings singleton and shared constants so
that other modules can simply do:

    from app.config.settings import get_settings
    from app.config import constants
"""
